apply plugin:'java'

compileJava {
    options.debug = "${System.env.TS_DEBUG}" == 'true' ? true : false
}

sourceSets {
    main {
        java {
            srcDir 'src'
        }
    }
}

dependencies {
    compile fileTree(dir: 'lib', include: ['*.jar'])
}
